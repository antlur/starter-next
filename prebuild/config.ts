import path from "path";

export const publicDir = path.join(process.cwd(), "public");

export const faviconDir = path.join(publicDir, "favicon");

export const storageDir = path.join(process.cwd(), "storage");
